apiVersion: v1
kind: Service
metadata:
  name: conexao-solidaria-campaign-api
  namespace: conexao-solidaria
spec:
  selector:
    app: conexao-solidaria-campaign-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
  type: NodePort
